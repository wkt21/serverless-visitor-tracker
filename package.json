{
  "name": "serverless-visitor-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless GitHub visitor tracker (Cloudflare Workers + KV)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
